// const express = require("express");
// const router = express.Router();
// const { uploadPodcast, getPodcasts, deletePodcast } = require("../controllers/podcastController");
// const { upload } = require("../middleware/upload");
//
// router.post("/summarize", upload.single("audio"), uploadPodcast);
// router.get("/", getPodcasts);
// router.delete("/:title", deletePodcast);
//
// module.exports = router;
const express = require("express");
